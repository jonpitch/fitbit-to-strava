language: node_js
node_js:
  - '10'

sudo: false
dist: trusty

cache:
  directories:
    - ~/.npm
    - ~/.cache

branches:
  only:
    - master

install:
  - set -e
  - npm ci

before_script:
  - set -e
  - npm run serve &

script:
  - set -e
  - npm run lint:js --quiet
  - npm run test
